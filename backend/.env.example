# Backend Environment Variables
PORT=5000
NODE_ENV=production

# Set this to your deployed frontend origin (scheme + host, no trailing slash)
# Example for Vercel:
SOCKET_CORS_ORIGIN=https://fls-planningpoker.vercel.app

# For production, set:
# NODE_ENV=production
# SOCKET_CORS_ORIGIN=https://your-frontend-domain.com